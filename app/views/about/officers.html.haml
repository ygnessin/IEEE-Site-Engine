:javascript
  $(document).ready(function() {
    $('.execs').last().css('padding-right', '0px');
    $('.activities').last().css('padding-right', '0px');
    $('.hope').last().css('padding-right', '0px');
    $('.indrel').last().css('padding-right', '0px');
    $('.io').last().css('padding-right', '0px');
    $('.prodev').last().css('padding-right', '0px');
    $('.techops').last().css('padding-right', '0px');
    $('.website').last().css('padding-right', '0px');
  });
  
%h1 Current Officers
%div#about_nav
  - %w(information officers advisor contact).each do |name|
    = link_to name.upcase, name, :id => "about_#{name}", :class => "about_nav_link"

%h2 Executives
%hr
%br
- @execs.each do |officer|
  .officer.execs
    - relative_path = "officer_pictures/2013/#{officer[0].downcase.gsub(' ', '_')}.jpg"
    - path_prefix = "#{Dir.getwd}/app/assets/images/"
    - pn = Pathname.new(path_prefix + relative_path)
    - if pn.exist?
      = image_tag(relative_path, :class => "officer_pic")
    - else
      = image_tag("officer_pictures/2013/default.jpg", :class => "officer_pic")
    %div(class="title" style="opacity:0")
      = officer[0]
      %br
      = officer[1]
%br
%br

%h2{:style => "clear:left;"} Activities
%hr
%br
- @activities.each do |officer|
  .officer.activities
    - relative_path = "officer_pictures/2013/#{officer.downcase.gsub(' ', '_')}.jpg"
    - path_prefix = "#{Dir.getwd}/app/assets/images/"
    - pn = Pathname.new(path_prefix + relative_path)
    - if pn.exist?
      = image_tag(relative_path, :class => "officer_pic")
    - else
      = image_tag("officer_pictures/2013/default.jpg", :class => "officer_pic")
    %div(class="title" style="opacity:0")
      = officer
      - if @activities.index(officer) == 0
        %br
        Director

%h2{:style => "clear:left;"} HOPE
%hr
%br
- @hope.each do |officer|
  .officer.hope.officer_three
    - relative_path = "officer_pictures/2013/#{officer.downcase.gsub(' ', '_')}.jpg"
    - path_prefix = "#{Dir.getwd}/app/assets/images/"
    - pn = Pathname.new(path_prefix + relative_path)
    - if pn.exist?
      = image_tag(relative_path, :class => "officer_pic")
    - else
      = image_tag("officer_pictures/2013/default.jpg", :class => "officer_pic")
    %div(class="title" style="opacity:0")
      = officer
      - if @hope.index(officer) == 0
        %br
        Director
%br
%br

%h2{:style => "clear:left;"} Industrial Relations
%hr
%br
- @indrel.each do |officer|
  .officer.indrel
    - relative_path = "officer_pictures/2013/#{officer.downcase.gsub(' ', '_')}.jpg"
    - path_prefix = "#{Dir.getwd}/app/assets/images/"
    - pn = Pathname.new(path_prefix + relative_path)
    - if pn.exist?
      = image_tag(relative_path, :class => "officer_pic")
    - else
      = image_tag("officer_pictures/2013/default.jpg", :class => "officer_pic")
    %div(class="title" style="opacity:0")
      = officer
      - if @indrel.index(officer) == 0
        %br
        Director
%br
%br

%h2{:style => "clear:left;"} I/O
%hr
%br
- @io.each do |officer|
  .officer.io
    - relative_path = "officer_pictures/2013/#{officer.downcase.gsub(' ', '_')}.jpg"
    - path_prefix = "#{Dir.getwd}/app/assets/images/"
    - pn = Pathname.new(path_prefix + relative_path)
    - if pn.exist?
      = image_tag(relative_path, :class => "officer_pic")
    - else
      = image_tag("officer_pictures/2013/default.jpg", :class => "officer_pic")
    %div(class="title" style="opacity:0")
      = officer
      - if @io.index(officer) == 0
        %br
        Director

%h2{:style => "clear:left;"} Professional Development
%hr
%br
- @prodev.each do |officer|
  .officer.prodev.officer_four
    - relative_path = "officer_pictures/2013/#{officer.downcase.gsub(' ', '_')}.jpg"
    - path_prefix = "#{Dir.getwd}/app/assets/images/"
    - pn = Pathname.new(path_prefix + relative_path)
    - if pn.exist?
      = image_tag(relative_path, :class => "officer_pic")
    - else
      = image_tag("officer_pictures/2013/default.jpg", :class => "officer_pic")
    %div(class="title" style="opacity:0")
      = officer
      - if @prodev.index(officer) == 0
        %br
        Director

%h2{:style => "clear:left;"} Technical Operations
%hr
%br
- @techops.each do |officer|
  .officer.techops.officer_six
    - relative_path = "officer_pictures/2013/#{officer.downcase.gsub(' ', '_')}.jpg"
    - path_prefix = "#{Dir.getwd}/app/assets/images/"
    - pn = Pathname.new(path_prefix + relative_path)
    - if pn.exist?
      = image_tag(relative_path, :class => "officer_pic")
    - else
      = image_tag("officer_pictures/2013/default.jpg", :class => "officer_pic")
    %div(class="title" style="opacity:0")
      = officer
      - if @techops.index(officer) == 0
        %br
        Director

%h2{:style => "clear:left;"} Website
%hr
%br
- @website.each do |officer|
  .officer.website.officer_six
    - relative_path = "officer_pictures/2013/#{officer.downcase.gsub(' ', '_')}.jpg"
    - path_prefix = "#{Dir.getwd}/app/assets/images/"
    - pn = Pathname.new(path_prefix + relative_path)
    - if pn.exist?
      = image_tag(relative_path, :class => "officer_pic")
    - else
      = image_tag("officer_pictures/2013/default.jpg", :class => "officer_pic")
    %div(class="title" style="opacity:0")
      = officer
      - if @website.index(officer) == 0
        %br
        Director
